rootProject.name = "i18n-keyless-kotlin-example"

// The library from the repository, substituted for the published coordinates: the example
// always builds against the local source. A real app drops this line and pulls it from
// Maven Central.
includeBuild("../../ports/kotlin")
