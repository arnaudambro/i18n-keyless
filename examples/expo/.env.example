# Real-life setup: get an API key at https://i18n-keyless.com and copy this to `.env`.
# Expo exposes EXPO_PUBLIC_* vars to the app. Leave empty to use the local mock backend
# (from a device/emulator, use your machine's LAN IP instead of localhost).
EXPO_PUBLIC_I18N_KEYLESS_API_KEY=
