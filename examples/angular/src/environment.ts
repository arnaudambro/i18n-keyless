// Real-life setup: get an API key at https://i18n-keyless.com and put it here (or read it
// from your own environment files). Leave it empty to run offline against the local mock
// backend (examples/_mock-server) instead.
export const I18N_KEYLESS_API_KEY = "";
